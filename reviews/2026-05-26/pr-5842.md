---
pr_number: 5842
title: "frontend: fix react-hooks/rules-of-hooks in AuthVisible"
author: "Naga15"
url: "https://github.com/kubernetes-sigs/headlamp/pull/5842"
verdict: "REQUEST_CHANGES"
size_category: "Small"
files_changed: 1
additions: 9
deletions: 10
re_review: true
date: "2026-05-26"
findings_count:
  critical: 0
  warning: 1
  suggestion: 0
  positive: 2
---

# PR #5842: frontend: fix react-hooks/rules-of-hooks in AuthVisible

**Author**: [@Naga15](https://github.com/Naga15) ·
**Branch**: `fix/AuthVisible-hooks` ·
**Changes**: +9 / -10 across 1 file ·
**Size**: Small ·
Re-review since 2026-05-25

[View on GitHub →](https://github.com/kubernetes-sigs/headlamp/pull/5842)

## Verdict: REQUEST_CHANGES

## Summary

The hooks violation is now properly fixed: `isValidVerb` is computed before any hook call, the two `useQuery`/`useEffect` hooks are unconditional, and the early `return null` is deferred to after all hooks. Optional chaining on `itemClass?.apiName`/`itemClass?.apiVersion` is a nice defensive addition. The only blocker is the commit subject still missing the required SubArea segment.

## Findings

### Warnings
- **[HIGH]** `commit subject` — `frontend: Fix react-hooks/rules-of-hooks in AuthVisible` is missing the SubArea segment. Project convention requires `area: SubArea: Description`, e.g. `frontend: AuthVisible: Fix react-hooks/rules-of-hooks violation`. The verb is now correctly capitalised (✓), but the SubArea is still absent — @illume will REQUEST_CHANGES on this.

### Positive
- The `enabled: !!item && isValidVerb` guard on `useQuery` correctly suppresses the network call when the verb is invalid, without calling hooks conditionally.
- Removing both `// eslint-disable-next-line react-hooks/rules-of-hooks` suppressions in favour of a proper structural fix is the right approach.

## Linked Issues
