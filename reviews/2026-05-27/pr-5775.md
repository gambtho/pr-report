---
pr_number: 5775
title: "frontend: Fix Backstage receiver listener leak on Home remount"
author: "Rohith-Saran"
url: "https://github.com/kubernetes-sigs/headlamp/pull/5775"
verdict: "REQUEST_CHANGES"
size_category: "Small"
files_changed: 2
additions: 11
deletions: 5
re_review: true
date: "2026-05-27"
findings_count:
  critical: 0
  warning: 2
  suggestion: 0
  positive: 2
---

# PR #5775: frontend: Fix Backstage receiver listener leak on Home remount

**Author**: [@Rohith-Saran](https://github.com/Rohith-Saran) ·
**Branch**: `bug#5417` ·
**Changes**: +11 / -5 across 2 files ·
**Size**: Small ·
Re-review since 2026-05-22

[View on GitHub →](https://github.com/kubernetes-sigs/headlamp/pull/5775)

## Verdict: REQUEST_CHANGES

## Summary

The listener-leak fix is correct and now also addresses the Copilot JSDoc comments that @illume flagged. The function now returns `(): void` as cleanup, the `useEffect` in `Home` returns it, and a non-Backstage code path also returns a no-op so the return type is always `() => void`. However two of the three commits are still missing the required SubArea segment.

## Findings

### Warnings
- **[HIGH]** `commit: "frontend: Fix Backstage receiver listener leak on Home remount"` — Missing SubArea; should be e.g. `frontend: Home: Fix Backstage receiver listener leak on remount`.
- **[HIGH]** `commit: "frontend: Fix JSDoc for setupBackstageMessageReceiver"` — Missing SubArea; should be e.g. `frontend: backstageMessageReceiver: Fix JSDoc for setupBackstageMessageReceiver`.

### Positive
- **[Positive]** `Home/index.tsx:116` — `return setupBackstageMessageReceiver()` correctly wires the cleanup into `useEffect`'s return value, so React calls it on unmount.
- **[Positive]** `backstageMessageReceiver.ts` — Both the Backstage and non-Backstage code paths now return a stable `() => void` function, making the return type contract complete and safe.
