---
pr_number: 5785
title: "frontend: themes: Replace deprecated MediaQueryList listener methods"
author: "YadavAkhileshh"
url: "https://github.com/kubernetes-sigs/headlamp/pull/5785"
verdict: "REQUEST_CHANGES"
size_category: "Small"
files_changed: 2
additions: 44
deletions: 7
re_review: true
date: "2026-05-29"
findings_count:
  critical: 0
  warning: 1
  suggestion: 0
  positive: 2
---

# PR #5785: frontend: themes: Replace deprecated MediaQueryList listener methods

**Author**: [@YadavAkhileshh](https://github.com/YadavAkhileshh) ·
**Branch**: `fix-deprecated-media-query` ·
**Changes**: +44 / -7 across 2 files ·
**Size**: Small ·
Re-review since 2026-05-28

[View on GitHub →](https://github.com/kubernetes-sigs/headlamp/pull/5785)

## Verdict: REQUEST_CHANGES

## Summary
The implementation is now correct and complete: `addEventListener/removeEventListener` is used as primary path with a legacy `addListener/removeListener` fallback for older WebViews, and the tests cover both paths. The only remaining block is the first commit subject still missing the `themes:` SubArea segment — the same issue flagged in every previous review.

## Findings

### Warnings
- **[HIGH]** `commit subject` — First commit `"frontend: Replace deprecated MediaQueryList.addListener/removeListene…"` is missing the `themes:` SubArea. The correct format is `"frontend: themes: Replace deprecated MediaQueryList listener methods"`. The second commit has the SubArea correctly (`frontend: themes: Add fallback test coverage…`). Please amend the first commit.

### Positive
- Handler type narrowed from `MediaQueryListEvent | MediaQueryList` to `MediaQueryListEvent` — the legacy fallback path now correctly detects `addEventListener` absence at runtime, so the union type was unnecessary and removal is clean.
- Tests cover both the modern `addEventListener` path and the legacy `addListener` fallback path, and both verify the exact handler reference is passed to the cleanup call.
