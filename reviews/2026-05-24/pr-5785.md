---
pr_number: 5785
title: "frontend: themes: Replace deprecated MediaQueryList listener methods"
author: "YadavAkhileshh"
url: "https://github.com/kubernetes-sigs/headlamp/pull/5785"
verdict: "APPROVE"
size_category: "Small"
files_changed: 2
additions: 25
deletions: 14
re_review: true
date: "2026-05-24"
findings_count:
  critical: 0
  warning: 0
  suggestion: 1
  positive: 3
---

# PR #5785: frontend: themes: Replace deprecated MediaQueryList listener methods

**Author**: [@YadavAkhileshh](https://github.com/YadavAkhileshh) ·
**Branch**: `fix/replace-deprecated-mediaquery-listener` ·
**Changes**: +25 / -14 across 2 files ·
**Size**: Small ·
Re-review since 2026-05-22 (head SHA updated)

[View on GitHub →](https://github.com/kubernetes-sigs/headlamp/pull/5785)

## Verdict: APPROVE

## Summary

This PR correctly replaces the deprecated `MediaQueryList.addListener`/`removeListener` API with the standard `addEventListener('change', ...)`/`removeEventListener('change', ...)` in `usePrefersColorScheme`. The type annotation is appropriately narrowed from `MediaQueryListEvent | MediaQueryList` to `MediaQueryListEvent`. Tests are updated and meaningfully improved.

The new head SHA introduces a single squashed commit with the same correct logic; the only minor concern is a commit message format deviation from the project convention.

## Findings

### Suggestions
- **[LOW]** `frontend/src/lib/themes.ts` (commit message) — The commit subject `frontend: Replace deprecated MediaQueryList.addListener/removeListener in usePrefersColorScheme` is missing the SubArea segment and is 91 characters long (soft limit is 72). The PR title already follows the correct format: `frontend: themes: Replace deprecated MediaQueryList listener methods` (63 chars). Consider aligning the commit message to the PR title before merge.

### Positive
- The test for `'should register and clean up the media query listener'` was significantly improved: it now verifies (1) `addEventListener` is called with the `'change'` event name, and (2) `removeEventListener` is called with the exact same handler reference (`mock.calls[0][1]`). This catches any accidental inline-function regression where cleanup would fail to remove the listener.
- Type narrowing from `MediaQueryListEvent | MediaQueryList` to `MediaQueryListEvent` is correct — standard event listeners only ever receive `MediaQueryListEvent`, so removing the union eliminates dead code and improves type safety.
- CLA signed; no merge conflicts; clean, focused diff with full test coverage.

## Linked Issues
- #5784 — Replace deprecated MediaQueryList listener methods
