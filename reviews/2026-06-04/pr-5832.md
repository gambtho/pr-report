---
pr_number: 5832
title: "backend: Reject unsupported Jaeger tracing endpoint"
author: "harrshita123"
url: "https://github.com/kubernetes-sigs/headlamp/pull/5832"
verdict: "REQUEST_CHANGES"
size_category: "Small"
files_changed: 4
additions: 75
deletions: 20
re_review: true
date: "2026-06-04"
findings_count:
  critical: 0
  warning: 2
  suggestion: 0
  positive: 3
---

# PR #5832: backend: Reject unsupported Jaeger tracing endpoint

**Author**: [@harrshita123](https://github.com/harrshita123) ·
**Branch**: `fix-5799-jaeger-endpoint` ·
**Changes**: +75 / -20 across 4 files ·
**Size**: Small ·
Re-review since 2026-05-25

[View on GitHub →](https://github.com/kubernetes-sigs/headlamp/pull/5832)

## Verdict: REQUEST_CHANGES

## Summary

The content of this PR is correct: `validateTracing()` is extracted cleanly, the Jaeger-endpoint rejection logic is sound (rejected at validation unless stdout is also enabled, and rejected at runtime in `createTracingExporter`), and the test cases cover both scenarios. However, the branch has picked up a merge commit (`Merge branch 'main' into fix-5799-jaeger-endpoint`) since the last review, and the commit subject is still missing a SubArea segment. Please rebase onto `main` to remove the merge commit.

## Findings

### Warnings
- **[HIGH]** `git history` — PR branch contains a merge commit: `Merge branch 'main' into fix-5799-jaeger-endpoint`. Project convention requires rebasing onto main rather than merging. Please run `git rebase origin/main` and force-push to clean the branch history.
- **[MEDIUM]** `commit subject` — `backend: Reject unsupported Jaeger tracing endpoint` is missing a SubArea. The change spans `pkg/config` and `pkg/telemetry`; suggest `backend: telemetry: Reject unsupported Jaeger tracing endpoint`.

### Positive
- `backend/pkg/config/config.go` — `validateTracing()` cleanly extracted as a helper; the validation rejects Jaeger when stdout is not enabled, which is consistent with runtime precedence in `createTracingExporter`.
- `backend/pkg/telemetry/telemetry.go` — Runtime guard `if isJaegerConfigured { return nil, fmt.Errorf(...) }` placed after the stdout early-return, so the documented "stdout takes priority over Jaeger" behavior is correctly preserved.
- `backend/pkg/config/config_test.go` and `telemetry_test.go` — New test cases cover both the rejection path and the stdout-priority path, including the runtime error message format.

## Linked Issues
- #5799 — Jaeger endpoint silently routes through OTLP exporter
