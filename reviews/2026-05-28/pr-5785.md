---
pr_number: 5785
title: "frontend: themes: Replace deprecated MediaQueryList listener methods"
author: "YadavAkhileshh"
url: "https://github.com/kubernetes-sigs/headlamp/pull/5785"
verdict: "REQUEST_CHANGES"
size_category: "Small"
files_changed: 2
additions: 28
deletions: 12
re_review: true
date: "2026-05-28"
findings_count:
  critical: 0
  warning: 2
  suggestion: 0
  positive: 2
---

# PR #5785: frontend: themes: Replace deprecated MediaQueryList listener methods

**Author**: [@YadavAkhileshh](https://github.com/YadavAkhileshh) ·
**Branch**: `fix/replace-deprecated-mediaquery-listener` ·
**Changes**: +28 / -12 across 2 files ·
**Size**: Small ·
Re-review since 2026-05-23

[View on GitHub →](https://github.com/kubernetes-sigs/headlamp/pull/5785)

## Verdict: REQUEST_CHANGES

## Summary
The implementation is now correct: `addEventListener('change', handler)` is used with the modern API, the legacy `addListener` fallback is retained for older WebViews, and the handler type is properly narrowed to `MediaQueryListEvent`. The tests are thorough and now verify the exact event name and handler reference on both add and remove. However the commit subject is still missing the required SubArea segment and exceeds the 72-character soft limit.

## Findings

### Warnings
- **[HIGH]** `commit` — Commit subject missing SubArea segment: `frontend: Replace deprecated MediaQueryList.addListener/removeListener in usePrefersColorScheme`. Project convention requires `area: SubArea: Description`, e.g. `frontend: themes: Replace deprecated MediaQueryList listener methods`. This has been flagged in previous reviews.
- **[MEDIUM]** `commit` — Commit subject is ~90 characters, exceeding the 72-character soft limit. Shortening to match the PR title would fix both issues at once: `frontend: themes: Replace deprecated MediaQueryList listener methods`.

### Positive
- The `typeof mql.addEventListener === 'function'` guard before branching cleanly handles browsers/WebViews where the modern API isn't present, preserving the legacy path without breaking change.
- Tests now assert the exact event name `'change'` and verify that `removeEventListener` is called with the same handler reference captured from `addEventListener.mock.calls[0][1]` — much stronger than just checking call counts.
