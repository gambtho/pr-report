---
pr_number: 5843
title: "frontend: Export ApiError from K8s package for plugin access"
author: "Rohith-Saran"
url: "https://github.com/kubernetes-sigs/headlamp/pull/5843"
verdict: "REQUEST_CHANGES"
size_category: "Small"
files_changed: 6
additions: 36
deletions: 1
re_review: false
date: "2026-05-25"
findings_count:
  critical: 0
  warning: 2
  suggestion: 0
  positive: 1
---

# PR #5843: frontend: Export ApiError from K8s package for plugin access

**Author**: [@Rohith-Saran](https://github.com/Rohith-Saran) ·
**Branch**: `export-api-error` ·
**Changes**: +36 / -1 across 6 files ·
**Size**: Small

[View on GitHub →](https://github.com/kubernetes-sigs/headlamp/pull/5843)

## Verdict: REQUEST_CHANGES

## Summary

Fixes a real plugin crash (`Cannot read properties of undefined (reading 'ApiError')`) by re-exporting `ApiError` from the K8s package barrel and adding a Vite external mapping. The code change is minimal and correct. However, the commit subject is missing the required SubArea segment, and the storyshot files included in the diff contain unrelated structural changes that need to be separated or explained.

## Findings

### Warnings
- **[HIGH]** `frontend/src/lib/k8s/index.ts` — Commit subject `"frontend: Export ApiError from K8s package for plugin access"` is missing the required SubArea segment. Project convention is `area: SubArea: Description` (e.g. `frontend: k8s: Export ApiError from K8s package for plugin access`). Please squash/amend before merge.
- **[MEDIUM]** `frontend/src/components/common/Table/__snapshots__/`, `frontend/src/components/gateway/__snapshots__/`, `frontend/src/components/jobset/__snapshots__/` — Three storyshot files were regenerated with structural ARIA-element additions (`aria-live="polite"` divs, "No results found" text) that are unrelated to the ApiError export. These appear to be snapshot drift from a base-branch change. Please regenerate storyhots from a clean `main` checkout and remove the unrelated diff, or confirm these changes are intentional and explain them in the PR description.

### Positive
- Minimal change to fix the plugin crash: `export { ApiError }` in `k8s/index.ts` plus the Vite external mapping `'@kinvolk/headlamp-plugin/lib/k8s/api/v2/ApiError': 'pluginLib.K8s'`. The mapping correctly resolves deep-path imports to `pluginLib.K8s.ApiError`, as confirmed by the updated `pluginLib.snapshot`.

## Linked Issues
- #5821 — ApiError not accessible from window.pluginLib.K8s
