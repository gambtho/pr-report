---
pr_number: 5832
title: "backend: Reject unsupported Jaeger tracing endpoint"
author: "harrshita123"
url: "https://github.com/kubernetes-sigs/headlamp/pull/5832"
verdict: "APPROVE"
size_category: "Medium"
files_changed: 4
additions: 86
deletions: 20
re_review: true
date: "2026-06-06"
findings_count:
  critical: 0
  warning: 0
  suggestion: 1
  positive: 3
---

# PR #5832: backend: Reject unsupported Jaeger tracing endpoint

**Author**: [@harrshita123](https://github.com/harrshita123) ·
**Branch**: `fix-5799-jaeger-endpoint` ·
**Changes**: +86 / -20 across 4 files ·
**Size**: Medium ·
Re-review since 2026-06-05

[View on GitHub →](https://github.com/kubernetes-sigs/headlamp/pull/5832)

## Verdict: APPROVE

## Summary

The author has rebased cleanly — the two merge commits that blocked previous approvals are gone and the branch is now a single clean commit. The implementation correctly rejects Jaeger endpoints at both the config-validation layer (`validateTracing()`) and the telemetry-creation layer (`createTracingExporter`), with comprehensive tests covering all relevant scenarios. This is ready to merge.

## Findings

### Suggestions
- **[Suggestion|LOW]** `backend/pkg/config/config.go` — Commit subject is missing a SubArea segment. Current: `backend: Reject unsupported Jaeger tracing endpoint`. Preferred: `backend: config: Reject unsupported Jaeger tracing endpoint` (or `backend: telemetry: ...`). Minor only.

### Positive
- **[Positive]** Author rebased the branch to a single clean commit — the two merge-main commits that prompted the previous `REQUEST_CHANGES` are resolved. 🎉
- **[Positive]** `backend/pkg/config/config_test.go` + `telemetry_test.go`: Three new config validation test cases and one telemetry-layer test case provide thorough coverage: stdout-only path, Jaeger-without-stdout rejection, Jaeger-with-stdout (stdout wins), and telemetry-layer rejection independent of config validation.
- **[Positive]** The `jaeger-endpoint` flag is now registered with a self-documenting description `"Jaeger collector endpoint (unsupported; use otlp-endpoint)"` so users who pass `--jaeger-endpoint` get a clear startup error rather than a silent OTLP fallback.

## Linked Issues
- #5799 — Jaeger tracing endpoint should be rejected with a clear error
