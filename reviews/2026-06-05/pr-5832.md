---
pr_number: 5832
title: "backend: Reject unsupported Jaeger tracing endpoint"
author: "harrshita123"
url: "https://github.com/kubernetes-sigs/headlamp/pull/5832"
verdict: "REQUEST_CHANGES"
size_category: "Small"
files_changed: 4
additions: 75
deletions: 20
re_review: true
date: "2026-06-05"
findings_count:
  critical: 0
  warning: 2
  suggestion: 0
  positive: 3
---

# PR #5832: backend: Reject unsupported Jaeger tracing endpoint

**Author**: [@harrshita123](https://github.com/harrshita123) ·
**Branch**: `fix-5799-jaeger-endpoint` ·
**Changes**: +75 / -20 across 4 files ·
**Size**: Small ·
Re-review since 2026-06-04

[View on GitHub →](https://github.com/kubernetes-sigs/headlamp/pull/5832)

## Verdict: REQUEST_CHANGES

## Summary
The content of this PR is solid: it correctly rejects Jaeger endpoints at both the config-validation layer and the runtime telemetry layer, adds the `--jaeger-endpoint` flag with a clear "unsupported" description, and includes tests covering both paths. However, the branch now contains **two** merge commits from main, and the commit subject is still missing the SubArea segment — both issues were flagged in the previous review and have not been addressed.

## Findings

### Warnings
- **[HIGH]** `commit history` — Branch still contains merge commits: `54db56aa "Merge branch 'main' into fix-5799-jaeger-endpoint"` and `95a03698 "Merge branch 'main' into fix-5799-jaeger-endpoint"`. A second merge commit was added since the 2026-06-04 REQUEST_CHANGES. Must rebase onto current main and squash/clean the history before merge.
- **[MEDIUM]** `commit subject` — Subject `"backend: Reject unsupported Jaeger tracing endpoint"` is missing the SubArea segment per project convention. Expected format: `backend: config: Reject unsupported Jaeger tracing endpoint` (or `backend: telemetry:`).

### Positive
- Clean refactor: extracting `validateTracing()` into its own method with early-return structure improves testability and readability.
- Correct dual-layer rejection: config validation rejects Jaeger-only configurations early; `createTracingExporter` handles the edge case where Jaeger+stdout passes validation (stdout takes priority, Jaeger is rejected at runtime if reached alone).
- Good test coverage: regression tests added to both `config_test.go` and `telemetry_test.go` covering the new rejection paths.

## Linked Issues
- #5799 — Jaeger endpoint silently routing through OTLP
