---
pr_number: 5748
title: "Automatically switch theme based on OS theme"
author: "rforced"
url: "https://github.com/kubernetes-sigs/headlamp/pull/5748"
verdict: "REQUEST_CHANGES"
size_category: "Very Large"
files_changed: 128
additions: 797
deletions: 3183
re_review: true
date: "2026-05-20"
findings_count:
  critical: 0
  warning: 4
  suggestion: 2
  positive: 4
---

# PR #5748: Automatically switch theme based on OS theme

**Author**: [@rforced](https://github.com/rforced) ·
**Branch**: `auto-theme-switch` ·
**Changes**: +797 / -3183 across 128 files ·
**Size**: Very Large (bulk of diff is regenerated storyshots) ·
Re-review since 2026-05-19 (previous verdict: NEEDS_DISCUSSION)

[View on GitHub →](https://github.com/kubernetes-sigs/headlamp/pull/5748)

## Verdict: REQUEST_CHANGES

## Summary

The PR adds an `auto` theme that follows the OS light/dark preference, correctly resolving it via
`getThemeName` and `usePrefersColorScheme`. The storyshots have been regenerated (addressing the
main gap from the previous review), and four focused unit tests were added for auto-resolution
logic. However, three hard blockers prevent merging: the PR is still marked WIP, has merge conflicts
(`needs-rebase`), and the author has not signed the CNCF CLA. Two minor code issues also need
addressing.

## Findings

### Warnings

- **[HIGH]** `(PR labels)` — `do-not-merge/work-in-progress` label is present; the author explicitly
  marked this as not ready to merge. Please remove once the PR is ready for final review.

- **[HIGH]** `(PR labels)` — `cncf-cla: no` — @rforced has not signed the CNCF Contributor License
  Agreement. The CLA must be signed before any Kubernetes SIG repo can accept a contribution.
  See https://git.k8s.io/community/CLA.md.

- **[HIGH]** `(PR labels)` — `needs-rebase` — the branch has merge conflicts with HEAD. After
  rebasing, the storyshots will likely need to be regenerated again.

- **[MEDIUM]** `frontend/src/App.tsx:52` — The `// eslint-disable-next-line react-hooks/exhaustive-deps`
  comment above the `useMemo` call is carried over from the previous code. Now that `osColorScheme`
  has been added to the dependency array, the rule may no longer fire. Verify whether the suppression
  is still needed; if not, remove it to keep the codebase clean and prevent future false negatives.

### Suggestions

- **[MEDIUM]** `frontend/src/lib/themes.ts:565` — An orphaned JSDoc block `/** Hook gets theme based
  on user preference, and also OS/Browser preference. */` was left in the diff after refactoring. It
  now immediately precedes the second (replacement) JSDoc for `getThemeName`, creating duplicate
  documentation. The stale comment should be removed.

- **[LOW]** `frontend/src/components/App/themeSlice.ts:113` — In `useCurrentAppTheme`, the return
  value of `usePrefersColorScheme()` is intentionally discarded (it is called only for its
  state-update side effect, which triggers a re-render when OS preference changes). A short inline
  comment (e.g. `// trigger re-render on OS scheme change`) would clarify the intent and avoid
  confusing future readers who might wonder why the return value is unused.

### Positive

- The storyshots have been regenerated, directly addressing the gap noted in the previous review.
  The large diff (−3183 lines) is almost entirely storyshot churn — the real logic changes are
  compact.

- `getThemeName` was cleanly refactored: extracting `prefersDarkColorScheme()` and
  `getOsColorScheme()` eliminates the duplicate `window.matchMedia` calls and makes the fallback
  logic more readable and deterministic.

- Four well-targeted unit tests were added in `themes.test.ts` covering the auto-to-dark,
  auto-to-light, and backend-default override paths.

- `ThemePreview` now shows a split half-light / half-dark preview swatch for the `auto` theme entry
  in the Settings panel — a polished UX touch that communicates the feature's intent at a glance.

## Linked Issues

No linked issue found in PR body (author noted "Didn't see one").
